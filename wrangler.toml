name = "throbbing-bush-8f59"
main = "worker.js"
compatibility_date = "2024-09-23"

# D1: Cloudflare ダッシュボードの既存バインディング名 DB と一致させること。
# database_id はダッシュボード → D1 → 対象DB → Settings で確認し、下記を実際の値に置き換えてから deploy してください。
# [[d1_databases]]
# binding = "DB"
# database_name = "YOUR_D1_DATABASE_NAME"
# database_id = "YOUR_D1_DATABASE_ID"

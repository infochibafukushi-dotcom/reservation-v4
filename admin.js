function pad(v){return String(v).padStart(2,"0")}function formatDate(d){const x=new Date(d);return`${x.getFullYear()}-${pad(x.getMonth()+1)}-${pad(x.getDate())}`}function addDays(d,n){const x=new Date(d);x.setDate(x.getDate()+n);return x}function jaDay(d){return["日","月","火","水","木","金","土"][d.getDay()]}function slotKey(date,time){return`${date}_${time}`}function applyGridColumns(grid,daysCount){const isMobile=window.matchMedia("(max-width:640px)").matches;const timeCol=isMobile?44:60;grid.style.gridTemplateColumns=isMobile?`${timeCol}px repeat(${daysCount}, minmax(62px, 1fr))`:`${timeCol}px repeat(${daysCount}, minmax(112px, 1fr))`}
const adminState={page:0,blocks:[],blockSet:new Set(),reservations:[],menu:[]};function yenAdmin(n){return`${Number(n||0).toLocaleString("ja-JP")}円`}function adminDates(){const today=new Date();today.setHours(0,0,0,0);return Array.from({length:7},(_,i)=>addDays(today,adminState.page*7+i))}function adminTimes(){const out=[];for(let h=6;h<=21;h++){for(let m=0;m<60;m+=30){if(h===21&&m>0)continue;out.push(`${pad(h)}:${pad(m)}`)}}return out}async function adminLogin(){const password=document.getElementById("adminPassword").value.trim();try{const res=await apiPost(ENDPOINTS.login,{password});if(!res.success)return toast("パスワードが違います");sessionStorage.setItem("admin_auth","1");document.getElementById("loginArea").classList.add("hidden");document.getElementById("adminView").classList.remove("hidden");await adminLoadAll()}catch(e){toast(e.message||"ログイン失敗")}}async function adminLoadAll(){await Promise.all([adminLoadBlocks(),adminLoadReservations(),adminLoadSettings(),adminLoadMenu()])}async function adminLoadBlocks(){const data=await apiGet(ENDPOINTS.getBlocks);adminState.blocks=data.blocks||[];adminState.blockSet=new Set(adminState.blocks.map(b=>slotKey(b.date,b.time)));adminRenderCalendar()}async function adminLoadReservations(){const data=await apiGet(ENDPOINTS.getReservations);adminState.reservations=data||[];renderReservationTable();renderStats()}async function adminLoadSettings(){try{const data=await apiGet(ENDPOINTS.getSettings);document.getElementById("notifyWebhook").value=data.settings?.notify_webhook_url||""}catch{}}async function adminLoadMenu(){try{const m=await apiGet(ENDPOINTS.getMenu);adminState.menu=[...(m.move_type||m.vehicle||[]).map(x=>({...x,group:"move_type"})),...(m.assist||[]).map(x=>({...x,group:"assist"})),...(m.stairs||[]).map(x=>({...x,group:"stairs"})),...(m.equipment||[]).map(x=>({...x,group:"equipment"})),...(m.round||[]).map(x=>({...x,group:"round"}))]}catch{adminState.menu=[]}renderMenuEditor()}function adminRenderCalendar(){const grid=document.getElementById("adminCalendarGrid"),range=document.getElementById("adminDateRange"),dates=adminDates(),times=adminTimes();applyGridColumns(grid,dates.length);grid.innerHTML="";range.textContent=`${formatDate(dates[0]).replaceAll("-","/")} - ${formatDate(dates[6]).slice(5).replace("-","/")}`;const corner=document.createElement("div");corner.className="time-label sticky-corner";corner.textContent="時間";grid.appendChild(corner);dates.forEach(d=>{const date=formatDate(d);const h=document.createElement("button");h.className=`date-header ${[0,6].includes(d.getDay())?"weekend":""}`;h.innerHTML=`<span>${d.getMonth()+1}/${d.getDate()}</span><small>${jaDay(d)}</small>`;h.addEventListener("click",()=>toggleDay(date));grid.appendChild(h)});times.forEach(time=>{const t=document.createElement("div");t.className="time-label";t.textContent=time;grid.appendChild(t);dates.forEach((d,idx)=>{const date=formatDate(d),blocked=adminState.blockSet.has(slotKey(date,time));const cell=document.createElement("button");cell.type="button";cell.className=`slot-cell ${blocked?"slot-ng":(idx%2?"slot-alt":"slot-ok")}`;cell.textContent=blocked?"×":"◎";cell.addEventListener("click",()=>toggleSlot(date,time,blocked));grid.appendChild(cell)})});document.getElementById("adminPrevWeek").disabled=adminState.page<=0}async function toggleSlot(date,time,blocked){await apiPost(ENDPOINTS.blockSlot,{date,time,mode:blocked?"unblock":"block"});await adminLoadBlocks()}async function toggleDay(date){await apiPost(ENDPOINTS.blockDay,{date,mode:"toggle"});await adminLoadBlocks()}function renderStats(){const list=adminState.reservations||[],active=list.filter(r=>String(r.status||"active")==="active").length,done=list.filter(r=>String(r.status||"")==="done").length,sales=list.reduce((sum,r)=>sum+Number(String(r.estimate||"0").replace(/[^\d]/g,"")||0),0);document.getElementById("statCount").textContent=list.length;document.getElementById("statActive").textContent=active;document.getElementById("statDone").textContent=done;document.getElementById("statSales").textContent=yenAdmin(sales)}function renderReservationTable(){document.getElementById("reservationList").innerHTML=(adminState.reservations||[]).map(r=>`<div class="res-row"><strong>${escapeHtml(r.name||"")}</strong> <span class="status-pill">${escapeHtml(r.status||"active")}</span><div>${escapeHtml(r.phone||"")}</div><div>${escapeHtml(r.date||"")} ${escapeHtml(r.time||"")}</div><div>${escapeHtml(r.pickup||"")} → ${escapeHtml(r.destination||"")}</div><div>${escapeHtml(r.vehicle||"")} / ${escapeHtml(r.assist||"")} / ${escapeHtml(r.stairs||"")} / ${escapeHtml(r.equipment||"")} / ${escapeHtml(r.roundTrip||"")}</div><div>${escapeHtml(r.estimate||"")}</div><div class="admin-actions"><button class="secondary-btn" onclick="updateStatus('${escapeHtml(r.id)}','done')">完了</button><button class="secondary-btn" onclick="updateStatus('${escapeHtml(r.id)}','active')">未対応</button><button class="danger-btn" onclick="cancelReservation('${escapeHtml(r.id)}')">キャンセル</button></div></div>`).join("")||"予約なし"}async function updateStatus(id,status){await apiPost(ENDPOINTS.updateReservation,{id,status});await adminLoadReservations()}async function cancelReservation(id){if(!confirm("キャンセルしますか？"))return;await apiPost(ENDPOINTS.cancelReservation,{id});await adminLoadAll()}function renderMenuEditor(){document.getElementById("menuEditor").innerHTML=(adminState.menu||[]).map((m,i)=>`<div class="menu-row"><label>名称<input data-menu-name="${i}" value="${escapeHtml(m.name||"")}"></label><label>価格<input data-menu-price="${i}" value="${Number(m.price||0)}"></label><label>区分<select data-menu-group="${i}"><option value="move_type"${m.group==="move_type"?" selected":""}>移動方法</option><option value="assist"${m.group==="assist"?" selected":""}>介助</option><option value="stairs"${m.group==="stairs"?" selected":""}>階段</option><option value="equipment"${m.group==="equipment"?" selected":""}>機材</option><option value="round"${m.group==="round"?" selected":""}>往復待機</option></select></label><button class="danger-btn" onclick="deleteMenuItem(${i})">削除</button></div>`).join("")}function deleteMenuItem(i){adminState.menu.splice(i,1);renderMenuEditor()}function addMenuItem(){adminState.menu.push({name:"新規項目",price:0,group:"move_type"});renderMenuEditor()}function collectMenu(){return adminState.menu.map((m,i)=>({name:document.querySelector(`[data-menu-name="${i}"]`).value,price:Number(document.querySelector(`[data-menu-price="${i}"]`).value||0),group:document.querySelector(`[data-menu-group="${i}"]`).value}))}async function saveMenu(){await apiPost(ENDPOINTS.saveMenu,{items:collectMenu()});toast("メニュー保存完了");await adminLoadMenu()}async function saveSettings(){await apiPost(ENDPOINTS.saveSettings,{notify_webhook_url:document.getElementById("notifyWebhook").value.trim(),new_password:document.getElementById("newPassword").value.trim()});document.getElementById("newPassword").value="";toast("設定保存完了")}function downloadCsv(){location.href=apiUrl(ENDPOINTS.getCsv)}document.addEventListener("DOMContentLoaded",()=>{document.getElementById("loginBtn").addEventListener("click",adminLogin);document.getElementById("adminPassword").addEventListener("keydown",e=>{if(e.key==="Enter")adminLogin()});document.getElementById("refreshAdminBtn").addEventListener("click",adminLoadAll);document.getElementById("downloadCsvBtn").addEventListener("click",downloadCsv);document.getElementById("saveSettingsBtn").addEventListener("click",saveSettings);document.getElementById("addMenuBtn").addEventListener("click",addMenuItem);document.getElementById("saveMenuBtn").addEventListener("click",saveMenu);document.getElementById("adminPrevWeek").addEventListener("click",()=>{adminState.page=Math.max(0,adminState.page-1);adminLoadBlocks()});document.getElementById("adminNextWeek").addEventListener("click",()=>{adminState.page+=1;adminLoadBlocks()});if(sessionStorage.getItem("admin_auth")==="1"){document.getElementById("loginArea").classList.add("hidden");document.getElementById("adminView").classList.remove("hidden");adminLoadAll()}});
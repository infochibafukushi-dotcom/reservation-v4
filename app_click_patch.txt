// 既存app.jsに追加する変更点（削除なし・◎クリック処理だけ差し替え）
//
// 1) ◎セルを作っている箇所の onclick / addEventListener を以下に変更
//
// box.onclick = () => {
//   const q = new URLSearchParams({
//     date: ymd(d),
//     time: ('0'+h).slice(-2)+':' + (m===0 ? '00' : '30')
//   });
//   location.href = 'form-step1.html?' + q.toString();
// };
//
// 2) 既存の◎×カレンダーUI・週送り・深夜ボタン・デザインはそのまま保持
